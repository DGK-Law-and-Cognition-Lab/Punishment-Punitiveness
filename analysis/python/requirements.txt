# =============================================================================
# Python requirements for the NLP pipeline
# =============================================================================
# Tested with Python 3.10+
# A GPU is strongly recommended for notebook 02 (DeBERTa zero-shot classification)
# Two notebooks need API keys:
#   - 02_classification.ipynb         needs ANTHROPIC_API_KEY
#   - 03_embeddings_and_similarity.ipynb  needs VOYAGE_API_KEY

# ---- Core data + ML ----
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ---- NLP: classifiers and embeddings ----
sentence-transformers>=2.7      # MPNet, BGE, etc.
transformers>=4.40              # DeBERTa-v3 zero-shot
torch>=2.1                      # required by transformers; GPU build recommended

# ---- API-based models ----
anthropic>=0.40                 # Claude Haiku 4.5 forced-choice classifier
voyageai>=0.3                   # voyage-3-large embeddings

# ---- Topic modeling and dimensionality reduction ----
bertopic>=0.16
umap-learn>=0.5.5
hdbscan>=0.8.33

# ---- Sentiment + lexicons ----
vaderSentiment>=3.3.2
empath>=0.89

# ---- Visualization ----
matplotlib>=3.8
seaborn>=0.13
wordcloud>=1.9                  # for the by-tertile word cloud figure

# ---- Notebook environment ----
jupyter>=1.0
ipykernel>=6.29
nbconvert>=7.16                 # for exporting to HTML

# ---- Utilities ----
tqdm>=4.66
python-dotenv>=1.0              # so notebooks can load API keys from .env
